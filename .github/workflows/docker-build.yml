name: Build and publish Docker images

on:
  push:
    branches: [ "main", "dev" ]

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Choose tag
        id: tag
        run: |
          if [ "${GITHUB_REF_NAME}" = "main" ]; then
            echo "tag=prod" >> $GITHUB_OUTPUT
          else
            echo "tag=dev" >> $GITHUB_OUTPUT
          fi

      - name: Build and push scraper
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ghcr.io/chimeralaboratories/availability-scraper:${{ steps.tag.outputs.tag }}

      - name: Build and push browser
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/browser/Dockerfile
          push: true
          tags: ghcr.io/chimeralaboratories/availability-browser:${{ steps.tag.outputs.tag }}
